spring:
  application:
    name: library-books
  datasource:
    url: jdbc:h2:mem:librarydb;DB_CLOSE_DELAY=-1
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
  shell:
    interactive:
      enabled: true
